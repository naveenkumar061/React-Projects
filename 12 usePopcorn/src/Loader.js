// Loader Component: This component displays a loading message.

// Import React library for creating React components.
import React from "react";

// Loader function component.
function Loader() {
  // Return a paragraph element with the class name "loader" and the text "Loading...".
  return <p className="loader">Loading...</p>;
}

// Export the Loader component as the default export of this module.
export default Loader;
